{
    "name": "gold/trade",
    "description": "秒合约交易系统",
    "require": {
        "php": ">=7.2",
        "topthink/framework": "5.0.*"
    },
    "autoload": {
        "psr-4": {
            "app\\": "application/"
        }
    },
    "scripts": {
        "post-install-cmd": [
            "rm -rf public/uploads && mkdir public/uploads"
        ]
    }
}
